const userController = require('./users');
const videoController = require('./videos');
const tagController = require('./tags');
const thumbnailController = require('./thumbnails');
const shoppingCartController = require('./shoppingcarts');

module.exports = {
  userController,
  videoController,
  tagController,
  thumbnailController,
  shoppingCartController,
};
