class Quiz is subclass of Promotion			
		instance variables
			private quiz: seq of QA;
		
		operations
			public Quiz: String * set of Reward * Brand * seq of QA ==> Quiz
			Quiz(nam, sor, b, qas)== 
			(
					name := nam;
					rewards := sor;
					brand := b;
					quiz := qas;
			)
end Quiz
