
module array2_err
{

  function sum = sum_array(A[])
  {
	i=0;
	while(i < A.length) { // error 1
		sum=sum+A[i];
		i=i+1;
	}
  }

  function main()
  {

   N=16;
   a=[N];

   i=0;
   while(i << N) // error 2
   {
   	a[i] = 1;
	i=i+1;
	}

	sum=sum_array(a);
 	io.println("sum of array elements = ",sum);
 	}
}
